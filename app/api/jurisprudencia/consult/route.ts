// The legacy URL uses the same central authorization and execution service.
export { POST } from '@/app/api/research/route'
export const runtime = 'nodejs'
export const maxDuration = 60
export const dynamic = 'force-dynamic'
